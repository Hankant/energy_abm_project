# 能源生态系统 ABM (Agent-Based Model) 项目任务清单

- [x] **~第一阶段：核心框架搭建与代理心智实现~**
  - [x] ~实现 `model.py` 作为环境和调度中心，支持步进时间推演。~
  - [x] ~实现 `agents.py` 的异质性大模型个体与容错规则树降级保障。~
  - [x] ~设计并在 `agents.py` 实现基于（极端天气+价格偏离度+政策影响度）加权算法的历史记忆系统。~
  - [x] ~实现分层 Prompt 模板（Persona 静态层 + Daily Context 动态层）。~
  - [x] ~实现 `network.py` 小世界社交网络拓扑，提取邻居行动作为社会规范压力。~
  - [x] ~实现 `policy.py` 下的多元化政策剧本和信号广播。~
  - [x] ~编写全局统一运行脚本 `run.py` 以控制主循环。~
  - [x] ~编写 `README.md` 与深度架构指南 `visual_logic_guide.md`。~

- [ ] **第二阶段：问卷-论文-ABM 同构校准 (Research Alignment)**
  - [x] 新增 `docs/planning/research_alignment.md`，明确研究主线、WTP 构念边界和问卷-模型映射。
  - [ ] 将 WTP 拆分为低碳产品 WTP 与保供/可靠性 WTP，避免代码、问卷和论文表述混用。
  - [ ] 编写问卷 JSON/CSV 清洗脚本，将 v10 问卷输出转换为 Agent 初始化字段。
  - [ ] 修改 `agents.py`，使 Agent 输出从每日用电量和单一 WTP 扩展为节电强度、低碳产品采用概率、产品 WTP、可接受回本期和保供 WTP。
  - [ ] 用小样本跑通“问卷样本 -> Agent 初始化 -> 仿真输出 -> 论文假设检验”的最小闭环。

- [ ] **第三阶段：实验自动化 (Experiment Automation)**
  - [ ] 编写 `batch_run.py`，支持多情境、多参数场景（如：社交网络开启/关闭、干预政策有/无）的一键批量实验。
  - [ ] 定义完整的对照组 (Control Group) 和实验组 (Experimental Group)。

- [ ] **第四阶段：数据可视化与深度分析 (Data Visualization & Analysis)**
  - [ ] 编写 `analysis.py` 进行数据解析与清洗。
  - [ ] 宏观层面：绘制时间序列图，展现不同政策冲击下总耗电量、低碳产品平均 WTP 和采用率的变化。
  - [ ] 微观层面：绘制箱线图/散点图，拆解异质性（不同所在地区、不同收入群体）如何导致用电行为、WTP 和采用概率的分化。
  - [ ] 网络分析：通过对比开启与关闭社交网络的数据，分析“邻居压力”对个体决策带来的扩散影响规律。

- [ ] **第五阶段：模型优化与进阶 (Optional)**
  - [ ] 改造 `agents.py` 的 LLM 推理接口，引入 `asyncio` 和 `aiohttp` 以实现并发请求（显著提升群体仿真效率）。
  - [ ] 基于前序数据可视化结论，微调记忆重要性打分权重因子（使其符合真实心理学规律）。
